services:
  - type: web
    name: data-quality-radar
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run radar/app.py --server.port $PORT --server.address 0.0.0.0
    autoDeploy: true
